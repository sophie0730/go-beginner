package main

/*func main() {

	var i int = 10
	fmt.Println("i address=", &i)

	// var ptr *int = &i
	//1. ptr是指標變數
	//2. ptr的類型 *int
	//3. ptr本身的值&i
	var ptr *int = &i
	fmt.Println("ptr =", ptr)
	fmt.Println("ptr address=", &ptr) //儲存i address，也需要額外分配一塊記憶體空間，所以ptr也有自己的address
	fmt.Println("ptr指向的值=", *ptr)     //取出這個指標指向的記憶體位址的值
}*/
